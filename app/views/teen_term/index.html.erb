<%= render partial: "shared/admin_nav" %>
<div class="ma_back">	
	<div class="row ma_flashalert">
		<%= flash.notice %>
	</div>
	<div class="row">
		<% @teen_term.each do |fn| %>
			<ul>
				<li>
					<%= link_to fn.teen_id, teen_term_url(fn.id) %>
					<ul>
						<li><%= fn.updated_at.strftime("%m/%d/%Y") %></li>
						<li><%= link_to "Edit", edit_teen_term_url(fn.id) %></li>
						<li><%= link_to "Delete", teen_term_url(fn.id), method: :delete %></li>
					</ul>
				</li>
			</ul>
		<% end %>
	</div>
</div>