<%= render partial: "shared/admin_nav" %>
<div class="ma_back">
	<div class="row ma_flashalert">
		<%= flash.notice %>
	</div>
	<div class="row">
		<% @retreat.each do |fn| %>
			<ul>
				<li>
					<%= link_to fn.teen.full_name %><br><%= fn.updated_at.strftime("%m/%d/%Y") %>
				</li>
			</ul>
		<% end %>
	</div>
</div>