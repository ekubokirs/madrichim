<%= render partial: "shared/admin_nav" %>
<div class="ma_back">	
	<div class="row ma_flashalert">
		<%= flash.notice %>
	</div>
	<table class="row">
		<thead>
			<tr>
				<td>Mid-Year Evaluation</td>
				<td>Name</td>
				<td>Time Stamp</td>
				<td>Edit</td>
				<td>Delete</td>
			</tr>
		</thead>
		<tbody>
			<% @teenMY.each do |fn| %>
			<tr>
				<td><%= link_to fn.id, teen_mid_year_url(fn.id) %></td>
				<td><%= fn.teen.full_name %></td>
				<td><%= fn.updated_at %></td>
				<td><%= link_to "Edit", edit_teen_mid_year_url(fn.id) %></td>
				<td><%= link_to "Delete", teen_mid_year_url(fn.id), method: :delete %></td>
			</tr>
			<% end %>
		</tbody>
	</table>
</div>