<%= render partial: "shared/admin_nav" %>
<div class="ma_back">	
	<div class="row ma_flashalert">
		<%= flash.notice %>
	</div>
	<table class="row">
		<thead>
			<tr>
				<td>Teacher End Year Evaluation</td>
				<td>Name</td>
				<td>Teen Name</td>
				<td>Time Stamp</td>
				<td>Edit</td>
				<td>Delete</td>
			</tr>
		</thead>
		<tbody>
			<% @teachEY.each do |fn| %>
			<tr>
				<td><%= link_to fn.id, teacher_end_year_url(fn.id) %></td>
				<td><%= fn.user.full_name %></td>
				<td><%= fn.teen.full_name %></td>
				<td><%= fn.updated_at %></td>
				<td><%= link_to "Edit", edit_teacher_end_year_url(fn.id) %></td>
				<td><%= link_to "Delete", teacher_end_year_url(fn.id), method: :delete %></td>
			</tr>
			<% end %>
		</tbody>
	</table>
</div>