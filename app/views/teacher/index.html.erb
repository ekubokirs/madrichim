<%= render partial: "shared/admin_nav" %>
<div class="ma_back">	
	<div class="row">
  	<table class = "large-12 columns">
			<thead>
				<tr>
					<td>Name</td>
					<td>E-mail</td>
				</tr>
			</thead>
			<tbody>
				<% @teacher.each do |fn| %>
				<tr>
					<td><%= fn.full_name %></td>
					<td><%= fn.email %></td>
				</tr>
				<% end %>
			</tbody>
		</table>
  </div>
</div>